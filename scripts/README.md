# Вспомогательные скрипты разработчика и деплоя (бэкап БД, провижининг RPi).

Пока достаточно эмулятора MQTT-устройства для разработки дашборда без железа.
Реализация — этап 2 роадмапа (`scripts/mqtt_device_emulator.py`).
