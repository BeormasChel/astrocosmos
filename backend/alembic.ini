# Alembic будет инициализирован на этапе MVP-1 (схема БД).
# Запуск: alembic init alembic (уже размещено) → alembic revision --autogenerate
script_location = alembic
prepend_sys_path = .
version_path_separator = os

sqlalchemy.url = driver://user:pass@localhost/astrocosmos
